<Page
    x:Class="Mobile_Application.View.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mobile_Application.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator}, Path=Search}"
    >
    
    <Page.BottomAppBar>
        <CommandBar x:ConnectionId='1'  x:Name="appBar">
            <AppBarButton x:ConnectionId='2' x:Name="Account" Icon="Account" Command="{Binding Account}"/>
            <AppBarButton x:ConnectionId='3' x:Name="Favorite" Icon="Contact" Command="{Binding GoFavorite}"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:ConnectionId='4' x:Name="SearchPageGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Search"  Grid.Row="0"  Grid.Column="0" FontSize="34"  VerticalAlignment="Top" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="1">
            <RadioButton x:ConnectionId='6' Name="rb_Name"                          >By last name</RadioButton>
            <RadioButton x:ConnectionId='7' Name="rb_Email"                           >By Email</RadioButton>
            <RadioButton x:ConnectionId='8' Name="rb_Company"                             >By company</RadioButton>
        </StackPanel>

        <TextBox Text="{Binding Input, Mode=TwoWay}" Grid.Row="2" Grid.Column="0" Height="40" Width="300" VerticalAlignment="Center" HorizontalAlignment="Left"/>

        <Button x:ConnectionId='5' x:Name="Search" Content="Search" Command="{Binding Search}" Grid.Row="4" Grid.Column="0"  Height="40" Width="100" VerticalAlignment="Center" HorizontalAlignment="Left" />
   
    </Grid>
</Page>

