<Page
    x:Class="Mobile_Application.View.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mobile_Application.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator}, Path=Search}"
    >
    
    <Page.BottomAppBar>
        <CommandBar x:ConnectionId='1'  x:Name="appBar">
            <AppBarButton x:ConnectionId='2' x:Uid="account" x:Name="Account" Label="" Command="{Binding Account}">
                <AppBarButton.Icon>
                    <BitmapIcon UriSource="ms-appx:///Assets/ic_person_outline_white_36dp.png"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:ConnectionId='3' x:Uid="favorites" x:Name="Favorite" Label="" Command="{Binding GoFavorite}">
                <AppBarButton.Icon>
                    <BitmapIcon UriSource="ms-appx:///Assets/ic_contacts_white_36dp.png"/>
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:ConnectionId='4' x:Name="SearchPageGrid" Style="{StaticResource backgroundStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Search"  Grid.Row="0"  Grid.Column="0" FontSize="34"  VerticalAlignment="Top" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="1">
            <RadioButton x:ConnectionId='6' Name="rb_Name"                          >
                <TextBlock x:Uid="by_name" Text=""></TextBlock>
            </RadioButton>
            <RadioButton x:ConnectionId='7' Name="rb_Email"                           >
                <TextBlock x:Uid="by_email" Text=""></TextBlock>
            </RadioButton>
            <RadioButton x:ConnectionId='8' Name="rb_Company"                             >
                <TextBlock x:Uid="by_company" Text=""></TextBlock>
            </RadioButton>
        </StackPanel>

        <TextBox Text="{Binding Input, Mode=TwoWay}" Grid.Row="2" Grid.Column="0" Height="40" Width="300" VerticalAlignment="Center" HorizontalAlignment="Left"/>

        <Button x:ConnectionId='5' x:Uid="search" x:Name="Search" Content="" Command="{Binding Search}" Grid.Row="4" Grid.Column="0"  Height="40" Width="100" VerticalAlignment="Center" HorizontalAlignment="Left" />
   
    </Grid>
</Page>

